<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.companyname.dms.workbench.mapper.MoveIntoMapper">

    <delete id="deleteByStuId">
        delete from tb_move_into where stu_id=#{id}
    </delete>

    <insert id="saveStuAndDate">
        insert into tb_move_into values(null,#{stuId},#{moveIntDate})
    </insert>
</mapper>
